#!/usr/bin/env ruby
# encoding: utf-8

require 'bundler/setup'
utopia_data_path = File.join(File.expand_path('../lib', __FILE__))
$LOAD_PATH.unshift(utopia_data_path)

require 'utopia_data/cli'

UtopiaData::CLI.start(ARGV)
